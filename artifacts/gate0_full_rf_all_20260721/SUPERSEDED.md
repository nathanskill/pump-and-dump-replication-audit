# Superseded baseline run

This run remains immutable historical evidence, but it is not the release candidate. The source script was subsequently parameterised, so the `script_sha256` in this directory does not match the current source file even though the metrics are unchanged.

Use `../gate0_released_code_rf_all_v2_20260721/` for the current released-code baseline. Its environment records the final parameterised script hash and `min_samples_leaf=1` explicitly.
